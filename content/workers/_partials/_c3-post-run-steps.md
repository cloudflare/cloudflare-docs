---
_build:
  publishResources: false
  render: never
  list: never
inputParameters: templateCategory;;templateType;;language
---

For setup, select the following options:

- For *What would you like to start with?*, choose `$1`.
- For *Which template would you like to use?*, choose `$2`.
- For *Which language do you want to use?*, choose `$3`.
- For *Do you want to use git for version control?*, choose `Yes`.
- For *Do you want to deploy your application?*, choose `No` (we will be making some changes before deploying).