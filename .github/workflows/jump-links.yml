name: Check for broken internal jump links

on:
  pull_request:
    branches:
      - production

jobs:
  check-links:
    runs-on: ubuntu-latest
    steps:
      - name: Install dependencies
        run: npm install -g markdown-link-check
      - name: Check for broken internal jump links
        run: |
          markdown-link-check 'content/**/*.md' --config '{ "ignorePatterns": ["^https?://", "^mailto:", "^#", "^javascript:"] }'
