<figure
	class="rt-meeting-lifecycle not-content"
	aria-label="A RealtimeKit Meeting persists over time. The first participant joining creates an active Session. The Session ends shortly after everyone leaves, but the Meeting still exists. A participant can return later and create a new Session. Participant time is billable only during active Sessions."
>
	<div class="rt-canvas" aria-hidden="true">
		<div class="rt-meeting-scope">
			<div class="rt-scope-label">
				<span>Meeting</span>
				<span class="rt-persistent-badge">Persistent</span>
			</div>

			<div class="rt-meeting-header">
				<div>
					<span class="rt-eyebrow">Reusable virtual room</span>
					<strong>Weekly standup</strong>
				</div>
			</div>

			<div class="rt-timeline">
				<section class="rt-state rt-session">
					<div class="rt-state-heading">
						<span class="rt-status-dot"></span>
						<span>Session 1</span>
						<span class="rt-state-badge">Active</span>
					</div>
					<div class="rt-event">
						<span class="rt-event-marker"></span>
						<span>First participant joins</span>
					</div>
					<div class="rt-participants">
						<span>A</span>
						<span>B</span>
						<span>C</span>
						<small>3 participants</small>
					</div>
					<div class="rt-event rt-event-end">
						<span class="rt-event-marker"></span>
						<span>Ends shortly after everyone leaves</span>
					</div>
					<div class="rt-usage">Billable participant time</div>
				</section>

				<section class="rt-state rt-idle">
					<div class="rt-idle-icon">
						<svg viewBox="0 0 24 24" fill="none" stroke="currentColor">
							<circle cx="12" cy="12" r="8"></circle>
							<path d="M12 8v4l3 2"></path>
						</svg>
					</div>
					<strong>No active Session</strong>
					<span>Meeting still exists</span>
					<div class="rt-no-usage">No billable participant time</div>
				</section>

				<section class="rt-state rt-session">
					<div class="rt-state-heading">
						<span class="rt-status-dot"></span>
						<span>Session 2</span>
						<span class="rt-state-badge">Active</span>
					</div>
					<div class="rt-event">
						<span class="rt-event-marker"></span>
						<span>A participant joins later</span>
					</div>
					<div class="rt-participants">
						<span>A</span>
						<span>D</span>
						<small>2 participants</small>
					</div>
					<div class="rt-event rt-event-end">
						<span class="rt-event-marker"></span>
						<span>Ends shortly after everyone leaves</span>
					</div>
					<div class="rt-usage">Billable participant time</div>
				</section>
			</div>

			<div class="rt-time-axis">
				<span>Time</span>
				<div></div>
				<svg viewBox="0 0 12 12" fill="none" stroke="currentColor">
					<path d="m3 2 4 4-4 4"></path>
				</svg>
			</div>
		</div>
	</div>
</figure>

<style>
	.rt-meeting-lifecycle {
		--rt-background: var(--nb-background, #fff);
		--rt-surface: var(--nb-card, #fff);
		--rt-border: var(--nb-border, #dedee2);
		--rt-border-strong: var(--nb-border-strong, #b7b7be);
		--rt-text: var(--nb-foreground, #202023);
		--rt-muted: var(--nb-muted-foreground, #6f6f76);
		--rt-accent: color-mix(
			in oklch,
			var(--nb-primary, #f6821f) 58%,
			var(--rt-text)
		);
		--rt-accent-soft: color-mix(in srgb, var(--rt-accent) 10%, transparent);
		container-type: inline-size;
		margin: 1.5rem 0;
	}

	.rt-canvas {
		padding: 2rem 1.25rem 1.25rem;
		border: 1px solid var(--rt-border);
		border-radius: 0.75rem;
		background-color: var(--rt-background);
		background-image: radial-gradient(
			circle at 1px 1px,
			color-mix(in srgb, var(--rt-border-strong) 36%, transparent) 1px,
			transparent 0
		);
		background-size: 14px 14px;
	}

	.rt-meeting-scope {
		position: relative;
		max-width: 760px;
		margin: 0 auto;
		padding: 1.5rem 1rem 1rem;
		border: 1px dashed color-mix(in srgb, var(--rt-accent) 75%, transparent);
		border-radius: 0.75rem;
	}

	.rt-scope-label {
		position: absolute;
		top: -0.75rem;
		left: 1rem;
		display: flex;
		align-items: center;
		gap: 0.45rem;
		padding: 0.2rem 0.55rem;
		border: 1px solid var(--rt-accent);
		border-radius: 999px;
		background: var(--rt-background);
		color: var(--rt-accent);
		font-family: var(--nb-font-mono, ui-monospace, monospace);
		font-size: 0.6875rem;
		font-weight: 600;
		letter-spacing: 0.06em;
		text-transform: uppercase;
	}

	.rt-persistent-badge {
		padding-left: 0.45rem;
		border-left: 1px solid color-mix(in srgb, var(--rt-accent) 35%, transparent);
		color: var(--rt-muted);
	}

	.rt-meeting-header {
		display: flex;
		align-items: center;
		justify-content: space-between;
		gap: 1rem;
		padding: 0.85rem 1rem;
		border: 1px solid var(--rt-border);
		border-radius: 0.55rem;
		background: var(--rt-surface);
		box-shadow: 0 1px 2px rgb(0 0 0 / 4%);
		color: var(--rt-text);
	}

	.rt-meeting-header > div:first-child {
		display: flex;
		flex-direction: column;
		gap: 0.1rem;
	}

	.rt-meeting-header strong {
		font-size: 0.95rem;
	}

	.rt-eyebrow {
		color: var(--rt-muted);
		font-family: var(--nb-font-mono, ui-monospace, monospace);
		font-size: 0.6rem;
		letter-spacing: 0.08em;
		text-transform: uppercase;
	}

	.rt-timeline {
		position: relative;
		display: grid;
		grid-template-columns: minmax(0, 1fr) minmax(120px, 0.65fr) minmax(0, 1fr);
		align-items: stretch;
		gap: 0.8rem;
		margin-top: 1rem;
	}

	.rt-timeline::before {
		content: "";
		position: absolute;
		top: 50%;
		left: 0;
		right: 0;
		height: 1px;
		background: var(--rt-border-strong);
	}

	.rt-state {
		position: relative;
		z-index: 1;
		display: flex;
		min-width: 0;
		flex-direction: column;
		border: 1px solid var(--rt-border);
		border-radius: 0.55rem;
		background: var(--rt-surface);
		box-shadow: 0 1px 2px rgb(0 0 0 / 4%);
		color: var(--rt-text);
	}

	.rt-session {
		border-color: color-mix(in srgb, var(--rt-accent) 55%, var(--rt-border));
	}

	.rt-state-heading {
		display: flex;
		align-items: center;
		gap: 0.4rem;
		padding: 0.55rem 0.65rem;
		border-bottom: 1px solid var(--rt-border);
		font-size: 0.75rem;
		font-weight: 650;
	}

	.rt-status-dot {
		width: 0.45rem;
		height: 0.45rem;
		border-radius: 50%;
		background: var(--rt-accent);
		box-shadow: 0 0 0 3px var(--rt-accent-soft);
	}

	.rt-state-badge {
		margin-left: auto;
		color: var(--rt-accent);
		font-family: var(--nb-font-mono, ui-monospace, monospace);
		font-size: 0.58rem;
		letter-spacing: 0.05em;
		text-transform: uppercase;
	}

	.rt-event {
		display: flex;
		align-items: center;
		gap: 0.35rem;
		padding: 0.55rem 0.65rem 0;
		color: var(--rt-muted);
		font-size: 0.625rem;
	}

	.rt-event-marker {
		width: 0.35rem;
		height: 0.35rem;
		border: 1px solid var(--rt-accent);
		border-radius: 50%;
		background: var(--rt-surface);
	}

	.rt-event-end {
		padding-top: 0;
		padding-bottom: 0.55rem;
	}

	.rt-participants {
		display: flex;
		align-items: center;
		min-height: 3.4rem;
		padding: 0.55rem 0.65rem;
	}

	.rt-participants > span {
		display: grid;
		width: 1.6rem;
		height: 1.6rem;
		margin-right: -0.3rem;
		place-items: center;
		border: 2px solid var(--rt-surface);
		border-radius: 50%;
		background: color-mix(in srgb, var(--rt-accent) 14%, var(--rt-surface));
		color: var(--rt-accent);
		font-size: 0.625rem;
		font-weight: 700;
	}

	.rt-participants small {
		margin-left: auto;
		color: var(--rt-muted);
		font-size: 0.6rem;
	}

	.rt-usage,
	.rt-no-usage {
		margin-top: auto;
		padding: 0.35rem 0.65rem;
		border-top: 1px solid var(--rt-border);
		font-family: var(--nb-font-mono, ui-monospace, monospace);
		font-size: 0.56rem;
		font-weight: 600;
		letter-spacing: 0.05em;
		text-align: center;
		text-transform: uppercase;
	}

	.rt-usage {
		background: var(--rt-accent-soft);
		color: var(--rt-accent);
	}

	.rt-idle {
		align-items: center;
		justify-content: center;
		padding-top: 0.8rem;
		text-align: center;
	}

	.rt-idle-icon {
		width: 1.4rem;
		height: 1.4rem;
		margin-bottom: 0.35rem;
		color: var(--rt-muted);
	}

	.rt-idle-icon svg {
		display: block;
		width: 100%;
		height: 100%;
		stroke-width: 1.5;
	}

	.rt-idle strong {
		font-size: 0.7rem;
	}

	.rt-idle > span {
		padding: 0.2rem 0.5rem 0.8rem;
		color: var(--rt-muted);
		font-size: 0.6rem;
	}

	.rt-no-usage {
		width: 100%;
		color: var(--rt-muted);
	}

	.rt-time-axis {
		display: flex;
		align-items: center;
		gap: 0.4rem;
		margin-top: 0.7rem;
		color: var(--rt-muted);
		font-family: var(--nb-font-mono, ui-monospace, monospace);
		font-size: 0.58rem;
		letter-spacing: 0.05em;
		text-transform: uppercase;
	}

	.rt-time-axis div {
		flex: 1;
		height: 1px;
		background: var(--rt-border-strong);
	}

	.rt-time-axis svg {
		width: 0.75rem;
		height: 0.75rem;
		stroke-width: 1.5;
	}

	@container (max-width: 560px) {
		.rt-canvas {
			padding: 1.75rem 0.75rem 0.75rem;
		}

		.rt-meeting-scope {
			padding: 1.35rem 0.7rem 0.7rem;
		}

		.rt-timeline {
			grid-template-columns: 1fr;
			gap: 1.25rem;
		}

		.rt-timeline::before {
			top: 0;
			bottom: 0;
			left: 50%;
			right: auto;
			width: 1px;
			height: auto;
		}

		.rt-idle {
			min-height: 8rem;
		}

		.rt-time-axis {
			display: none;
		}
	}
</style>
