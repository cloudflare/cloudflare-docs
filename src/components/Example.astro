---
import { Card } from "~/components";
interface Props {
	title?: string;
}

const { title } = Astro.props;

let header = `Example`;
if (title) {
	header = title;
}
---

<Card title={header}>
	<slot />
</Card>
