---
import { z } from "astro:schema";
import CodeSnippet from "./RTKCodeSnippet";

const props = z.object({
	id: z.union([z.string(), z.array(z.string())]),
});

const { id } = props.parse(Astro.props);
---

<CodeSnippet client:load id={id}>
	<slot />
</CodeSnippet>
