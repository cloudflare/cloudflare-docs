---
import { z } from "astro:schema";
import ReleaseNotes from "./RTKReleaseNotes";

const props = z.object({
	type: z.enum(["ui-kit", "core"]),
});

const { type } = props.parse(Astro.props);
---

<ReleaseNotes client:load type={type}>
	<slot />
</ReleaseNotes>
