---
import { getEntry } from "astro:content";
import type { CollectionEntry } from "astro:content";

interface Props {
	product: CollectionEntry<"product-maturity">["id"];
}

const { product } = Astro.props;

const entry = await getEntry("product-maturity", product);

if (!entry) {
	throw new Error(
		`Product maturity entry not found for product="${product}" in ${Astro.url.pathname}`,
	);
}
---

{entry.data}
