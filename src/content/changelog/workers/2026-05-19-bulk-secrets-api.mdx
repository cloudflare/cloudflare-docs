---
title: New Workers Bulk Secrets API Endpoint
description: A new API endpoint lets you create, update, and delete multiple Worker secrets in a single request.
products:
  - workers
date: 2026-05-20
---

The new [`PATCH /secrets-bulk` endpoint](/api/resources/workers/subresources/scripts/subresources/secrets/methods/bulk_update/) lets you batch multiple secret operations into a single request:

```sh
PATCH /accounts/{account_id}/workers/scripts/{script_name}/secrets-bulk
```

Each request supports up to **25 total** operations, where each secret update and each secret deletion counts as one operation.
Previously, creating, updating, or deleting secrets required a separate API request for each secret change. 

The following request body creates/updates `API_KEY` and `DB_PASSWORD`, and deletes `OLD_SECRET`:
```json
{
	"secrets": {
		"API_KEY": { "type": "secret_text", "name": "API_KEY", "text": "my-api-key" },
		"DB_PASSWORD": { "type": "secret_text", "name": "DB_PASSWORD", "text": "my-db-password" },
		"OLD_SECRET": null
	}
}
```

- Create or update a secret by including it with a value.
- Delete a secret key by setting its value to `null`.
- Secrets not included in the request are **left unchanged**.


For usage details, refer to the [API documentation](/api/resources/workers/subresources/scripts/subresources/secrets/methods/bulk_update/).
