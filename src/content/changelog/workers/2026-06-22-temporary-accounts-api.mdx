---
title: Temporary account deployments through the REST API
description: Platforms can now create temporary preview accounts through the Cloudflare REST API and let users claim deployed Workers later.
products:
  - workers
date: 2026-06-22
---

Platforms can now create temporary preview accounts directly through the Cloudflare REST API. This lets AI agents and other deployment platforms create a live Worker before the user signs in to Cloudflare, then return a claim URL so the user can keep the deployment.

This expands the temporary account flow beyond `wrangler deploy --temporary`. Instead of invoking Wrangler, your backend can request a proof-of-work challenge, create a temporary preview account, deploy with the returned account ID and API token, and show the claim URL to the user.

```bash
curl "https://api.cloudflare.com/client/v4/provisioning/previews/challenge" \
  -X POST \
  -H "Content-Type: application/json" \
  --data '{}'

curl "https://api.cloudflare.com/client/v4/provisioning/previews" \
  -X POST \
  -H "Content-Type: application/json" \
  --data '{
    "termsOfService": "https://www.cloudflare.com/terms/",
    "privacyPolicy": "https://www.cloudflare.com/privacypolicy/",
    "acceptTermsOfService": "yes",
    "challengeToken": "<CHALLENGE_TOKEN>",
    "solution": {
      "checkpoints": "<BASE64_CHECKPOINTS>"
    }
  }'
```

The API response includes `account.id`, `account.apiToken`, and `claim.url`. Use the temporary account ID and API token with supported Workers API endpoints, then show the claim URL to the user. Claim URLs must be opened within 60 minutes.

For the full API flow, proof-of-work requirements, supported products, and limits, refer to [Claim deployments (temporary accounts)](/workers/platform/claim-deployments/#use-the-rest-api-directly). For the original Wrangler announcement, refer to [Temporary accounts for AI agent deployments](/changelog/post/2026-06-19-temporary-accounts-for-agents/).
