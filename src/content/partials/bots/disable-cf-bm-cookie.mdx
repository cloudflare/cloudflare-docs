---
{}

---

You can begin the process of disabling the `__cf_bm` cookie [via the API](/api/resources/bot_management/methods/update/). To fully disable the cookie, contact customer support.

:::note

The `__cf_bm` cookie cannot be disabled if you have Behavioral Analysis enabled.
:::
