---
{}
---

Check the [system requirements](/cloudflare-one/team-and-resources/devices/warp/download-warp/#linux). Package dependencies are the following: `curl`, `gpg`, `iptables`, `iptables-persistent`, `lsb-core`, and `sudo`.
