---
{}
---

You can install `cloudflared` as a system service on Linux and Windows. On macOS, install it as a launch agent that starts at login or a launch daemon that starts at boot. In most cases, we recommend running `cloudflared` as a service to keep your origin available.

Follow our guides to set up and run `cloudflared` as a service in your environment:
