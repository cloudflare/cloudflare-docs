---
{}

---

:::caution[Zone name restriction]

Do not configure a custom hostname on a zone which matches the zone name. This configuration will cause a 403 Forbidden error due to DNS override restrictions applied for security reasons.

For example, if your SaaS zone is `example.com`, do not create a custom hostname named `example.com`.

:::
