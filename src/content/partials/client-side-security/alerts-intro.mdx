---
params:
  - availabilityDetails?
---

import { Markdown } from "~/components";

Once you have activated client-side security's resource monitoring, you can set up one or more alerts informing you of relevant client-side changes on your zones. <Markdown text={props.availabilityDetails ?? ""} />
