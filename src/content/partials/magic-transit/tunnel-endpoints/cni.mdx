---
params:
  - magicName
  - cniPath
  - productLink
---

import { Markdown } from "~/components";

Beyond GRE and IPsec tunnels, you can also use Network Interconnect (CNI) to onboard your traffic to {props.magicName}. Refer to <a href={props.productLink}>{props.cniPath}</a> for more information.
