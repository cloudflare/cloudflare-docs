---
inputParameters: productName;;productPath;;productLink

---

import { Markdown } from "~/components"

Beyond GRE and IPsec tunnels, you can also use Network Interconnect (CNI) to onboard your traffic to {props.one}. Refer to <a href={props.three}>{props.two}</a> for more information.
