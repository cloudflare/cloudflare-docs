---
params:
  - skipLink?
---

import { Markdown } from "~/components";

:::note
Currently, you can only create and deploy custom rulesets (rulesets with `"kind": "custom"`) at the zone level for the <Markdown text={ props.skipLink ? "`http_request_firewall_custom`" : "[`http_request_firewall_custom`](/waf/account/custom-rulesets/create-api/)" } /> phase.
:::
