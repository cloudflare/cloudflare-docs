---
params:
  - productName
  - dashButtonPath
  - magicWord
---

import { Aside, DashButton, Markdown, Tabs, TabItem } from "~/components";

{props.productName} customers can run traceroutes to analyze the hop-by-hop Internet path and latency between Cloudflare's network and their own network.

{ props.magicWord === "WAN Tunnels" && (
  <>
    <Markdown
      text={`
To run a traceroute from a specific Cloudflare data center to your network:

1. Log in to [Cloudflare One](https://one.dash.cloudflare.com/) > **Insights**.
2. Go to **Network visibility** > **WAN connector health**.
3. Select the tunnel for the traceroute.
4. Select the three dots > **Traceroute details**.
      `}
      inline={false}
    />
  </>
)}

{ props.magicWord !== "WAN Tunnels" && (
  <>
    <Aside type="note">
      To change between the new dashboard experience and the old one, find the banner at the top of the screen.
    </Aside>

    To run a traceroute from a specific Cloudflare data center to your network:

    <Tabs syncKey="dashNewNav">
      <TabItem label="Old dashboard">
        <Markdown
          text={`
1. Go to ${props.productName}'s **Network health** page.
          `}
          inline={false}
        />

        <DashButton url={props.dashButtonPath} />

        <Markdown
          text={`
2. Expand a Magic Tunnel and find the Cloudflare data center from which to run the traceroute.
3. Select **Traceroute for details**.
          `}
          inline={false}
        />
      </TabItem>

      <TabItem label="New dashboard" icon="rocket">
        <Markdown
          text={`
1. Go to ${props.productName}'s **Network health** page.
          `}
          inline={false}
        />

        <DashButton url={props.dashButtonPath} />

        <Markdown
          text={`
2. Select the tunnel for the traceroute.
3. Select the three dots > **Traceroute details**.
          `}
          inline={false}
        />
      </TabItem>
    </Tabs>
  </>
)}

You can access detailed data from the traceroute, including:

- TTL Host
- AS Number
- [Packets](https://www.cloudflare.com/learning/network-layer/what-is-a-packet/) sent in the traceroute
- Average, minimum, and maximum latency
- Standard deviation of latency