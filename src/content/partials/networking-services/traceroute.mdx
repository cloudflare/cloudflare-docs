---
params:
  - greIpsecURL
  - warpURL
  - tracerouteURL
---

:::note[Run <code>traceroute</code>]
If you connect through <a href={props.greIpsecURL}>GRE</a>, <a href={props.greIpsecURL}>IPsec</a>, [CNI](/network-interconnect/), or <a href={props.warpURL}>WARP</a> and want to run `traceroute` to an endpoint behind a <a href="/cloudflare-one/networks/connectors/cloudflare-tunnel/">Cloudflare Tunnel</a>, you need to change some settings.

Refer to <a href={props.tracerouteURL}>Run `traceroute`</a> for more information.
:::
