---
params:
  - productName
  - wanName
---

import { DashButton, Markdown } from '~/components';

{ props.wanName === "WAN Tunnels" ? (
	<>
		<Markdown
	text={`
		1. Log in to <a href="https://one.dash.cloudflare.com/">Cloudflare One</a>, and go to **Networks**.
		2. Go to **Connectors** > **Appliances** > **Profiles**.
		3. Find the ${props.productName} you want to deactivate, select the three dots next to it > **Edit**.
	`}
	inline={false}
	/>
	</>
) : (
	<>
		<Markdown
	text={`
		1. In the Cloudflare dash, go to the **Configuration** page.
	`}
	inline={false}
	/>
		<DashButton url="/?to=/:account/magic-wan/configuration" />
		<Markdown
	text={`
		3. In the **${props.productName}** tab, find the ${props.productName} you want to deactivate, select the three dots next to it > **Edit**.
	`}
	inline={false}
	/>
	</>
)
}
4. In **Status**, select _Deactivated_ from the dropdown.
5. Select **Update**.
