---
params:
  - dashName
---

import { DashButton, Markdown } from "~/components";

{ props.dashName === "CF1 Cloudflare WAN" ? (
	<>
		<Markdown
	text={`
		1. Log in to the <a href="https://dash.cloudflare.com/">Cloudflare dashboard</a>, and go to **Networking** > **Connectors**.
		2. Go to **Appliances** > **Profiles**.
		3. Select your Appliance > **View analytics**.
	`}
	inline={false}
	/>
	</>
) : (
	<>
<Markdown
	text={`
		1. Go to the **Connectors** page.
		`}
	inline={false}
	/>
		<DashButton url="/?to=/:account/magic-networks/connections" />
		<Markdown
	text={`
		2. Go to the **Appliances** tab > **Appliances**, and select the Cloudflare One Appliance you want to check analytics for.
		3. Select **View analytics**.
	`}
	inline={false}
	/>
	</>
)
}