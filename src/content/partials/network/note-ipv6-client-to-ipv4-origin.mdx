---
params:
  - page?
---

:::note
To allow IPv6-only clients to connect to IPv4-only origin web servers, keep {props.page == "pseudo-ipv4" ? <a href="/network/ipv6-compatibility/">IPv6 compatibility</a> : "IPv6 compatibility"} enabled and configure {props.page == "pseudo-ipv4" ? "Pseudo IPv4" : <a href="/network/pseudo-ipv4/">Pseudo IPv4</a>}.
:::
