---
{}

---

import { APIRequest } from "~/components";

After applying the necessary changes, use the Validate Prefix endpoint to trigger the validation checks.

<APIRequest
	path="/accounts/{account_id}/addressing/prefixes/{prefix_id}/validate"
	method="POST"
/>
