---
{}
---

import { MetaInfo, Type } from "~/components";

`messages` <Type text="array" /> <MetaInfo text="required" />

An array of message objects. Each message has:

- `content` <Type text="string" /> - The message content.
- `role` <Type text="string" /> - The role: `user`, `system`, or `assistant`.

---

`stream` <Type text="boolean" /> <MetaInfo text="optional" />

Set to `true` to return a stream of results as they are generated. Defaults to `false`.

---

`ai_search_options` <Type text="object" /> <MetaInfo text="optional" />

Per-request overrides for retrieval and model behavior. Supports the following nested options:

- `retrieval.filters` <Type text="object" /> - Narrow down search results based on metadata. Refer to [Metadata filtering](/ai-search/configuration/indexing/metadata/) for syntax and examples.
- `retrieval.max_num_results` <Type text="number" /> - Maximum number of chunks to return. Defaults to `10`, maximum `50`.
- `retrieval.retrieval_type` <Type text="string" /> - One of `vector`, `keyword`, or `hybrid`.
- `retrieval.match_threshold` <Type text="number" /> - Minimum similarity score (0-1). Defaults to `0.4`.
- `cache.enabled` <Type text="boolean" /> - Override the instance-level cache setting for this request.
- `reranking.enabled` <Type text="boolean" /> - Override the instance-level reranking setting for this request.

---

For the full list of optional parameters, refer to the [Chat Completions API reference](/api/resources/ai_search/subresources/instances/methods/chat_completions/).
