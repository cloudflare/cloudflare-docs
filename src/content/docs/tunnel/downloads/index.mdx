---
pcx_content_type: reference
title: Downloads
sidebar:
  order: 9
---

import { Render } from "~/components";

Cloudflare Tunnel requires the installation of a lightweight daemon, `cloudflared`, to connect your infrastructure to Cloudflare. If you are [creating a tunnel through the dashboard](/tunnel/setup/), you can simply copy-paste the installation command shown in the dashboard.

To download and install `cloudflared` manually, use one of the following links.

<Render file="tunnel/downloads" product="cloudflare-one" />

To update `cloudflared`, refer to [these instructions](/tunnel/downloads/update-cloudflared/).
