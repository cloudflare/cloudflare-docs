---
pcx_content_type: how-to
title: Export to Logfire
sidebar:
  order: 1
---

import {WranglerConfig} from "~/components";


Logfire is an observability platform built for general and AI observability that helps you understand and debug your applications. By exporting your Cloudflare Workers application telemetry to Logfire, you can:
- Visualize traces to understand request flows and identify performance bottlenecks
- Query spans and traces in Pydantic Logfire
- Create custom metrics, alerts, and dashboards to monitor your Workers

todo: ![Trace view with execution time displayed on a timeline](~/assets/images/workers-observability/logfire-example.png)

This guide will walk you through exporting OpenTelemetry-compliant traces and logs to Pydantic Logfire from your Cloudflare Worker application.


## Prerequisites

Before you begin, ensure you have:

- An active [Logfire account](https://logfire.pydantic.dev/) (free tier available)
- A deployed Worker that you want to monitor

## Step 1: Get your Logfire API key

1. Log in to your [Logfire account](https://logfire.pydantic.dev/)
2. Select the Project you'd like to export data from
3. Navigate to the tab **Settings**
4. In the left sidebar, Under **Developer Settings**, click on **Write Tokens**
7. Configure your token by entering a descriptive identifier (e.g., `cloudflare-worker-writetoken`)
8. Click **Create**
9. **Important**: Copy the token immediately and store it safely, as you will not be able to see it again.

The token will look like: `pylf_v1_...`

## Step 2: Configure Cloudflare destinations

Now you'll create destinations in the Cloudflare dashboard that point to Logfire.

### Logfire OTLP endpoints

Logfire provides separate OTLP endpoints for traces and logs:
- **Traces**: `https://logfire-us.pydantic.dev/v1/traces`
- **Logs**: `https://logfire-us.pydantic.dev/v1/logs`

!!! note
	The examples above are for the US region. If you are using the EU region, use `https://logfire-eu.pydantic.dev/...` instead.

### Configure trace destination

1. Navigate to your Cloudflare account's [Workers Observability](https://dash.cloudflare.com/?to=/:account/workers-and-pages/observability/pipelines) section
2. Click **Add destination**
3. Configure your trace destination:
   - **Destination Name**: `logfire-traces` (or any descriptive name)
   - **Destination Type**: Select **Traces**
   - **OTLP Endpoint**: `https://logfire-us.pydantic.dev/v1/traces`
   - **Custom Headers**: Add the authentication header:
     - Header name: `Authorization`
     - Header value: Your Logfire write token (e.g., `pylf_v1_...`)
4. Click **Save**

### Configure logs destination

Repeat the process for logs:

1. Click **Add destination** again
2. Configure your logs destination:
   - **Destination Name**: `logfire-logs` (or any descriptive name)
   - **Destination Type**: Select **Logs**
   - **OTLP Endpoint**: `https://logfire-us.pydantic.dev/v1/logs`
   - **Custom Headers**: Add the authentication header:
     - Header name: `Authorization`
     - Header value: Your Logfire write token (same as above)
3. Click **Save**

## Step 3: Configure your Worker

With your destinations created in the Cloudflare dashboard, update your Worker's configuration to enable telemetry export.

<WranglerConfig>

```json
{
  "observability": {
    "traces": {
      "enabled": true,
      // Must match the destination name in the dashboard
      "destinations": ["logfire-traces"]
    },
    "logs": {
      "enabled": true,
      // Must match the destination name in the dashboard
      "destinations": ["logfire-logs"]
    }
  }
}
```

</WranglerConfig>

After updating your configuration, deploy your Worker for the changes to take effect.

:::note
It may take a few minutes after deployment for data to appear in [Pydantic Logfire](https://logfire.pydantic.dev/).
:::