---
title: Run commands in a sandbox
description: Run Python code or a test suite, stream command output, keep a process running after the request ends, or open an interactive shell in a Linux sandbox.
pcx_content_type: navigation
sidebar:
  order: 5
  label: Overview
  group:
    label: Run commands
products:
  - sandbox
  - containers
  - durable-objects
---

import { DirectoryListing } from "~/components";

Each guide starts from a Worker with a Durable Object that starts a container, and runs commands in the container with `exec()`. The guides differ in what happens to the output and how long the command runs.

<DirectoryListing descriptions />

For every option that `exec()` accepts, refer to [Execute commands](/containers/guides/execute-commands/) in the Containers documentation.
