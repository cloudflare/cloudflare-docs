---
title: Run commands in a sandbox
description: Run a test suite, stream command output, keep a process running after the request ends, or open an interactive shell in a Linux sandbox.
pcx_content_type: navigation
sidebar:
  order: 6
  label: Overview
  group:
    label: Run commands
products:
  - sandbox
  - containers
  - durable-objects
---

import { DirectoryListing } from "~/components";

Run commands in a Linux sandbox from your Worker: a test suite, a command whose output streams to a browser, a process that keeps running after the request ends, or an interactive shell.

<DirectoryListing descriptions />
