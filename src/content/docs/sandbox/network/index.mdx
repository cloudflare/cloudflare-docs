---
title: Credentials and network access
description: Add credentials in your Worker so the sandbox never holds them, and decide which services code in a sandbox can reach.
pcx_content_type: navigation
sidebar:
  order: 8
  label: Overview
  group:
    label: Credentials and network
products:
  - sandbox
  - containers
  - dynamic-workers
---

import { DirectoryListing } from "~/components";

Add credentials in your Worker so the sandbox never holds them, and decide which services code in a sandbox can reach.

<DirectoryListing descriptions />

For every outbound option in each environment, refer to [Handle outbound traffic](/containers/guides/outbound-traffic/) for Containers and [Egress control](/dynamic-workers/usage/egress-control/) for Dynamic Workers.
