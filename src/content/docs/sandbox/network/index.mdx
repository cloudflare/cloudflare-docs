---
title: Control network access from a sandbox
description: Decide which services code in a sandbox can reach, and add credentials in your Worker so the sandbox never holds them.
pcx_content_type: navigation
sidebar:
  order: 9
  label: Overview
  group:
    label: Control network access
products:
  - sandbox
  - containers
  - dynamic-workers
---

import { DirectoryListing } from "~/components";

Decide which services code in a sandbox can reach, and add credentials in your Worker so the sandbox never holds them. The Containers and Dynamic Workers guides cover every outbound option for each environment.

<DirectoryListing descriptions />
