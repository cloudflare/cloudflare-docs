---
title: Work with files in a sandbox
description: Move files between your Worker and a sandbox, keep files after the sandbox's container stops, and mount an R2 bucket.
pcx_content_type: navigation
sidebar:
  order: 6
  label: Overview
  group:
    label: Work with files
products:
  - sandbox
  - containers
  - durable-objects
---

import { DirectoryListing } from "~/components";

Move files between your Worker and a Linux sandbox, keep files after its container stops with a snapshot or a backup in R2, and mount an R2 bucket in the sandbox.

<DirectoryListing descriptions />
