---
title: Work with files in a sandbox
description: Move files between your Worker and a sandbox, mount an R2 bucket, and keep files after the sandbox's container stops.
pcx_content_type: navigation
sidebar:
  order: 7
  label: Overview
  group:
    label: Work with files
products:
  - sandbox
  - containers
  - durable-objects
---

import { DirectoryListing } from "~/components";

Move files between your Worker and a Linux sandbox, mount an R2 bucket in the sandbox, and keep files after its container stops with a snapshot or a backup in R2.

<DirectoryListing descriptions />
