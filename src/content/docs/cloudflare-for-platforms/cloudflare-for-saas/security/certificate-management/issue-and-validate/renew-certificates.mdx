---
pcx_content_type: how-to
title: Renew
description: Renew custom hostname certificates before their 90-day expiration.
products:
  - cloudflare-for-saas
sidebar:
  order: 3
head:
  - tag: title
    content: Renew certificates
---

import { Render } from "~/components";

Custom hostname certificates have a 90-day validity period and are available for renewal 30 days before their expiration.

The validation method selected when you create a custom hostname applies to initial certificate issuance. During an Automated Certificate Management Environment (ACME) renewal, Cloudflare selects a method based on the hostname's current configuration and eligibility.

You can explicitly change the method through the API. DigiCert uses a separate renewal workflow.

## HTTP validation

Cloudflare can automatically renew a custom hostname certificate through [HTTP validation](/cloudflare-for-platforms/cloudflare-for-saas/security/certificate-management/issue-and-validate/validate-certificates/http/) when all these conditions are true:

- The custom hostname is not a wildcard.
- The hostname is active.[^1]
- The hostname points to the SaaS provider.
- The hostname traffic is proxied through Cloudflare.
- The hostname does not use [Delegated DCV](/cloudflare-for-platforms/cloudflare-for-saas/security/certificate-management/issue-and-validate/validate-certificates/delegated-dcv/).

Cloudflare may use HTTP validation even if you selected TXT for initial issuance.

If automatic HTTP validation is unavailable, retrieve the custom hostname's current `ssl.method` and `ssl.validation_records`. Share the required HTTP or TXT records with the domain owner.

## TXT validation

Wildcard hostnames cannot use HTTP validation. They must use [TXT validation](/cloudflare-for-platforms/cloudflare-for-saas/security/certificate-management/issue-and-validate/validate-certificates/txt/).

Non-wildcard hostnames with properly configured [Delegated DCV](/cloudflare-for-platforms/cloudflare-for-saas/security/certificate-management/issue-and-validate/validate-certificates/delegated-dcv/) also remain TXT-based. Keep the delegation CNAME record in place for future renewals.

For manual TXT validation, use each certificate order's current `ssl.validation_records`. These records can change between certificate orders.

<Render file="txt-validation_preamble" product="cloudflare-for-platforms" />

<Render file="update-dcv-method" product="cloudflare-for-platforms" />

After this step, follow the normal steps for [TXT validation](/cloudflare-for-platforms/cloudflare-for-saas/security/certificate-management/issue-and-validate/validate-certificates/txt/).

:::note
To automate TXT placement for future certificate orders, consider [Delegated DCV](/cloudflare-for-platforms/cloudflare-for-saas/security/certificate-management/issue-and-validate/validate-certificates/delegated-dcv/).
:::

[^1]: Meaning Cloudflare could verify your customer's ownership of the hostname and the [hostname status](/cloudflare-for-platforms/cloudflare-for-saas/domain-support/hostname-validation/validation-status/) is active.
