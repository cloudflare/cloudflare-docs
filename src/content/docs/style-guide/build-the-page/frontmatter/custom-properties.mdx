---
title: Custom properties
sidebar:
  order: 4
description: Configure custom frontmatter properties.
products:
  - style-guide
---

import { Type, MetaInfo } from "~/components";
import BaseSchemaProperties from "~/components/BaseSchemaProperties.astro";

We have added specific custom [frontmatter](/style-guide/build-the-page/frontmatter/) properties to meet specific needs.

:::note
The `description` field is a Nimbus built-in, not a custom property, but it is required for all pages with a `pcx_content_type`. For writing guidance, refer to [Writing a description](/style-guide/build-the-page/frontmatter/#writing-a-description).
:::

## Properties

<BaseSchemaProperties />
