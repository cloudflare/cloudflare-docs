---
pcx_content_type: how-to
title: Manage routes
sidebar:
  order: 2
  label: Routes
---

import { Details, Render, Tabs, TabItem } from "~/components";

A route maps an IP address or hostname to a [Cloudflare One connector](/cloudflare-one/networks/connectors/cloudflare-tunnel/private-net/#connectors) installed on your private network. When a user connects to that IP or hostname through Cloudflare's network, Cloudflare will route their traffic down a secure tunnel to the corresponding resource your private network.

## Route types

Cloudflare One offers different types of routes depending on your installed connector. To understand the full setup requirements for a routing configuration, review the documentation links provided in the following summary table:

| Route type | cloudflared | WARP-to-WARP | WARP Connector | Magic WAN |
| ---------- | ------------ | ------------ | -------------- | -------- |
| CIDR route  | ✅ <br/> [Documentation](/cloudflare-one/networks/connectors/cloudflare-tunnel/private-net/cloudflared/connect-cidr/) | ❌| ✅ <br/> [Documentation](/cloudflare-one/networks/connectors/cloudflare-tunnel/private-net/warp-connector/site-to-site/)| ❌|
| Private hostname route | ✅ <br/> [Documentation](/cloudflare-one/networks/connectors/cloudflare-tunnel/private-net/cloudflared/connect-private-hostname/)| ❌ | ❌ | ❌ |
| Public hostname route |  ✅ <br/> [Documentation](/cloudflare-one/traffic-policies/egress-policies/egress-cloudflared/)| ❌ | ❌ | ❌ |
| Published application route | ✅ <br/> [Documentation](/cloudflare-one/networks/connectors/cloudflare-tunnel/routing-to-tunnel/) | ❌ | ❌ | ❌ |
| WAN route |  ❌ | ❌ | ❌ | ✅ <br/> [Documentation](/magic-wan/configuration/manually/how-to/configure-routes/)|

## Add a CIDR route

CIDR routes steer traffic destined to a specific IP address down a Cloudflare Tunnel (either `cloudflared` or WARP Connector).

To add a CIDR route to an existing tunnel:

1. In [Cloudflare One](https://one.dash.cloudflare.com), go to **Networks** > **Routes** > **CIDR**.
2. Select **Add CIDR route**.
3. In **CIDR**, enter the IP address or CIDR range that you wish to route through the tunnel (for example, `10.0.0.1` or `10.0.0.0/8`). This can be a private or public IP.
4. For **Tunnel**, select the Cloudflare Tunnel that is being used to connect your private network to Cloudflare.
5. (Optional) Under **Additional settings**, select a [virtual network](/cloudflare-one/networks/connectors/cloudflare-tunnel/private-net/cloudflared/tunnel-virtual-networks/) for this tunnel route.  This step is only needed if the route's IP/CIDR range overlaps with another route in your account. If you do not select a virtual network, the IP route will be assigned to the `default` network.
		:::note
		Virtual networks are only supported for `cloudflared` tunnels.
		:::
6. Select **Create**.

Cloudflare will now route requests to your private network. To enable client-side connectivity between WARP and Cloudflare, refer to the [cloudflared](/cloudflare-one/networks/connectors/cloudflare-tunnel/private-net/cloudflared/connect-cidr/#3-route-private-network-ips-through-warp) or
[WARP Connector](/cloudflare-one/networks/connectors/cloudflare-tunnel/private-net/warp-connector/site-to-site/#3-route-traffic-between-warp-connector-and-cloudflare) guides.

## Add a hostname route

Hostname routes steer traffic destined to a public or private hostname down a Cloudflare Tunnel.

To add a hostname route to an existing tunnel:

1. In [Cloudflare One](https://one.dash.cloudflare.com), go to **Networks** > **Routes** > **Hostname routes**.
2. Select **Create hostname route**.
3. In **Hostname**, enter the private or public hostname that represents your application (for example, `wiki.internal.local` or `app.bank.com`).
4. For **Tunnel**, select the Cloudflare Tunnel that is being used to connect your private network to Cloudflare.
5. Select **Create**.

Cloudflare will now route requests to your private network. To enable client-side connectivity between WARP and Cloudflare, refer to the [private hostname](/cloudflare-one/networks/connectors/cloudflare-tunnel/private-net/cloudflared/connect-private-hostname/) or [public hostname](/cloudflare-one/traffic-policies/egress-policies/egress-cloudflared/#3-route-network-traffic-through-warp) setup guides.

## Add a published application route

Published application routes allow you to expose a private application to the Internet, via a domain that you have connected to Cloudflare.

To add a published application route to an existing tunnel:

1.

## Add a WAN route

To route traffic from Cloudflare to your WAN network, refer to the [Magic WAN documentation](/magic-wan/configuration/manually/how-to/configure-routes/).

