---
pcx_content_type: how-to
description: Render SSH, VNC, and RDP applications in a browser.
products:
  - cloudflare-one
title: Browser rendering
sidebar:
  order: 3
tags:
- SSH
- RDP
---

import { Render } from "~/components";

Cloudflare can render SSH, VNC, and RDP applications in a browser without the need for client software or end-user configuration changes. For SSH and VNC, user email prefixes must match their username on the server, except when browser-based SSH falls back to password sign-in, which prompts for an account name. RDP leverages your existing Windows usernames and passwords for authenticating to the Windows server; Cloudflare does not manage any credentials on the Windows server.

## Limitations

- Browser rendering is only supported for [self-hosted public applications](/cloudflare-one/access-controls/applications/http-apps/self-hosted-public-app/), which require a public hostname. The tunnel behind the application can still connect to a server on a private IP or hostname.
- You can only use browser rendering on domains and subdomains, not on specific paths.
- <Render file="access/self-hosted-app/ssh-sessions" product="cloudflare-one" />
- Cloudflare uses TLS to secure the egress RDP connection to your Windows server. We do not currently validate the chain of trust.

## Turn on browser rendering

Browser rendering is turned on as part of each protocol's setup guide:

- [SSH](/cloudflare-one/networks/connectors/cloudflare-tunnel/use-cases/ssh/ssh-browser-rendering/)
- [VNC](/cloudflare-one/networks/connectors/cloudflare-tunnel/use-cases/vnc-browser-rendering/)
- [RDP](/cloudflare-one/networks/connectors/cloudflare-tunnel/use-cases/rdp/rdp-browser/)
